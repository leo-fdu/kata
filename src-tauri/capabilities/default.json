{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "kata-windows",
  "description": "Events and window closing for the main and settings windows",
  "windows": ["main", "settings"],
  "permissions": [
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
